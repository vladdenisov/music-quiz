giterminismConfigVersion: 1
config:
  allowUncommitted: true
  allowUncommittedTemplates:
    - ".werf/**/*.tmpl"

helm:
  allowUncommittedFiles:
    - ".helm/*.yml"
    - ".helm/*.yaml"
    - ".helm/Chart.lock"
    - ".helm/charts"
