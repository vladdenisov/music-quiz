apiVersion: v1
kind: Service
metadata:
  name: app
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetServicePort }}
